#usda 1.0
(
    defaultPrim = "Scene"
    upAxis = "Y"
)

def Xform "Scene" {
    def "Instanced" (
        references = @nested/direct-mesh-instanced.usda@</Asset>
    ) {
    }
    def "OrdinaryPrototype" (
        references = @nested/direct-mesh-instanced.usda@</Asset/PrototypeMesh>
    ) {
    }
}
