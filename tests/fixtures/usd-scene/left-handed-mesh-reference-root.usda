#usda 1.0
(
    defaultPrim = "Scene"
    upAxis = "Y"
)

def Xform "Scene" {
    def "Instanced" (
        references = @nested/left-handed-mesh-instanced.usda@</Asset>
    ) {
    }
    def "OrdinaryPrototype" (
        references = @nested/left-handed-mesh-instanced.usda@</Asset/PrototypeMesh>
    ) {
    }
}
